package com.gerken.sofa.bolt;

	// Begin imports 

import static org.junit.Assert.fail;

import java.io.ByteArrayOutputStream;
import java.io.ObjectOutputStream;
import java.util.HashMap;

import org.junit.Test;

import backtype.storm.task.OutputCollector;

import com.gerken.sofa.bean.*;
import com.gerken.sofa.mock.*;

	// End imports 

public class TermPredictorBoltTest  {
	
		// Begin declarations 

		// End declarations 

	@Test
	public void testSerialization() {
		
		try {
			new ObjectOutputStream(new ByteArrayOutputStream()).writeObject(new TermPredictorBolt() );
		} catch (Throwable t) {
			fail("Class TermPredictorBoltLogic is not serializable");
		}

	}

	@Test
	public void testReadFromMeaningfulTerms() {

		// Begin testReadFromMeaningfulTerms logic 

//		HashMap<String,Object> config = new HashMap<String,Object>();
//		MockedBoltOutputCollector collector = new MockedBoltOutputCollector();
//		
//		MockedTuple tuple = new MockedTuple("MeaningfulTerms", MeaningfulTerm.sample().asValues()); 
//		TermPredictorBolt bolt = new TermPredictorBolt();

//		bolt.prepare(config,null,new OutputCollector(collector));
//		bolt.execute(tuple);
//		bolt.cleanup()

			// Validate execution side effects by interrogating collector
					
		// End testReadFromMeaningfulTerms logic 

	}

	@Test
	public void testReadFromNewQuestionTerms() {

		// Begin testReadFromNewQuestionTerms logic 

//		HashMap<String,Object> config = new HashMap<String,Object>();
//		MockedBoltOutputCollector collector = new MockedBoltOutputCollector();
//		
//		MockedTuple tuple = new MockedTuple("NewQuestionTerms", NewQuestionTerm.sample().asValues()); 
//		TermPredictorBolt bolt = new TermPredictorBolt();

//		bolt.prepare(config,null,new OutputCollector(collector));
//		bolt.execute(tuple);
//		bolt.cleanup()

			// Validate execution side effects by interrogating collector
					
		// End testReadFromNewQuestionTerms logic 

	}

// Begin custom methods 


// End custom methods 

}
